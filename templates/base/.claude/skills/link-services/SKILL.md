---
description: Link two services so one can call the other's API. Use when a service needs to consume another service's endpoints, set up service-to-service communication, or use a typed client from another service.
---

!`bunx vrn skill link-services`
